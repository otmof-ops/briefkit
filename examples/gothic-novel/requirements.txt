# Gothic novel typesetter — Python dependencies
#
# Required:
reportlab>=4.0.0     # PDF generation pipeline
#
# Optional (for audit checks and verification):
PyMuPDF>=1.23.0      # text extraction from rendered PDFs (used by audit)
